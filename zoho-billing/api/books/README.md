# Zoho Books integration

- Sync customers as Books **contacts** (`contact_type=customer`)
- Sync invoices with line items, HSN, and tax IDs
- Sync customer payments against Books invoice IDs
- Deluge: `deluge/functions/Sync_Invoice_To_Books.dg`

Org ID query param required on every call: `?organization_id=`
